On load:
    Read the shortcuts file and populate the projects and categories list boxes.
    Make sure a listbox item from projects listbox is automatically selected. (preferably the first item)
    For the selected project list item, populate the categories listbox accordingly.

When edit shortcuts button is clicked, open the saved text file in a default text editor.

Date structure:
    Dictionary<
        projectName,
        Dictionary<categoryName, folderPath>
    >

A background thread will keep monitoring the db file modifications (after a every few miliseconds perhaps)
    How:
if it's modified, 